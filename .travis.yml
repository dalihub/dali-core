language: cpp
sudo: required
dist: trusty

compiler:
  - gcc

os:
  - linux

script:
  - cd build/tizen/
  - autoreconf --install
  - ./configure --enable-profile=UBUNTU --enable-gles=20
  - make -j7 install

